Task 13: NG8107 file-upload + page-header QA
=============================================
Date: 2026-07-29
Method: grep code inspection

file-upload.component.ts template:
  {{ acceptedTypes.join(', ') || 'Todos os tipos' }}
  ✓ Uses direct property access (no ?.)

page-header.component.ts template:
  @if (breadcrumbs.length) {
  ✓ Uses direct property access (no ?.)

VERDICT: PASS - Both expressions use direct property access, no ?.
